var numeroIngresado = prompt("Ingrese un número", "0");

if (numeroIngresado == 3) {
    document.write(`
    <h2>si es igual</h2>
    `)
}
else {
    document.write(`
            <h2>no es igual</h2>
            `)
}

